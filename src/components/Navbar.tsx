
import { NavBar } from "./navbar/NavBar";

// This file is kept for backward compatibility
// but it just imports and re-exports our new NavBar component
export default function Navbar() {
  return <NavBar />;
}
